# msale – A Marketplace App Built with Expo 👋

This is an [Expo](https://expo.dev) project created with [`create-expo-app`](https://www.npmjs.com/package/create-expo-app). It is a student-built secondhand marketplace for campus users.

## Get started

1. Install dependencies

   ```bash
   npm install
Start the app
npx expo start
You can run the app on:

development build
Android emulator
iOS simulator
Expo Go
About the Project

This app lets university seniors sell secondhand items (like books, furniture, etc.) to juniors through a simple mobile interface.

Learn more

Expo documentation
Expo GitHub
Expo Discord

---

## 🛠 Next Steps:
1. Replace the content of `README.md` with **your chosen version** above
2. Save the file
3. Run these commands in the terminal:

```bash
git add README.md
git commit -m "Resolved merge conflict in README"
git push -u origin main